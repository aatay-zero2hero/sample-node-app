{
  "name": "sample-node-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./index.js",
    "build": "mkdir -p build && cp -R node_modules src build"
  },
  "dependencies": {
    "express": "~4.16.3",
    "jade": "~1.11.0",
    "nconf": "^0.8.4"
  }
}
